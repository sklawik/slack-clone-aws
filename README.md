This project yet has to be continued, it would contain secure login using AWS Cognito, JWT and rest of AWS infrastructure 
followed by frontend in NextJS. It could also be applied with terraform.

Each data would be stored in .env files, and .env-template so you can copy-paste your own credentials.
